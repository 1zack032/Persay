# Menza Environment Variables
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# MongoDB Connection (required for data persistence)
# Get this from MongoDB Atlas: Connect -> Drivers -> Connection String
# Replace <password> with your actual password
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/menza?retryWrites=true&w=majority

# Flask Configuration
FLASK_DEBUG=false
FLASK_ENV=production
SECRET_KEY=change-this-to-a-random-secret-key

# Optional: For local development
# PORT=5000

